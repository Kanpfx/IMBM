# 执行校准的预测式 BM

## 1. 核心思想

当前 BM 根据游戏观测选择战术阶段并生成战略指导，但它并不显式判断“采用这项战略后，局面将如何变化”。新的 BM 将被设计为一个**执行校准的预测式规划器**（Execution-Calibrated Predictive BM，EC-BM）：

> BM 对少量候选战略进行动作条件预测，根据预期收益与风险选择当前战略；战略执行后，再结合 IM 和运行时的实际执行情况，用真实观测校准下一轮预测与决策。

核心闭环为：

$$
\text{当前认知}
\rightarrow
\text{候选战略}
\rightarrow
\text{预测未来}
\rightarrow
\text{选择并执行近期战略}
\rightarrow
\text{真实反馈与校准}
$$

预测必须参与战略选择，而不是在战略已经选定后附加一段对未来的描述。这是本设计区别于普通“阶段判断 + 战略指导”BM 的关键。

## 2. 当前认知与战略目标

SC2 是部分可观测环境，BM 看到的观测并不等于完整游戏状态。因此，BM 维护当前认知状态 $$b_t$$：它由最新真实观测、有限历史、上一周期的实际执行和已有敌情推断共同形成，表示 BM 对当前局面的最佳估计，而不是假定的真实状态。这里的 $$t$$ 表示一次 BM 决策时刻，$$H$$ 表示固定的游戏时间跨度。

$$
b_t = U(b_{t^-}, o_t, u_{t^-:t}^{runtime}, \text{recent history})
$$

其中 $$t^-$$ 表示上一次 BM 决策时刻。若没有显式概率模型和贝叶斯更新规则，本文只把 $$b_t$$ 称为结构化 belief estimate，而不宣称它是严格的 Bayesian posterior。

战术表继续提供长期方向和阶段先验。BM 根据当前认知将其具体化为近期战略目标 $$g_t$$，例如完成关键科技、稳定防守、扩大生产或发起压力。目标描述“希望局面如何发展”，不等同于对未来的预测。

敌方信息应保留不确定性。BM 可以同时考虑少量合理的敌方发展假设，而不是把未观测到的敌军视为不存在，也不需要虚构一个确定的隐藏状态。

## 3. 候选战略与动作条件预测

每次 BM 决策时，只生成少量具有实际差异的候选战略：

$$
A_t = \{a_t^{(1)}, a_t^{(2)}, \ldots, a_t^{(N)}\}
$$

候选战略应使用固定的结构化字段表达，使不同候选的预测和评分可以比较，而不是完全自由的自然语言描述。

候选中至少包括：

- 延续当前战略；
- 根据新局势进行调整；
- 必要时采用更安全的应对方案。

令 $$y_t$$ 表示从当前认知中选出的、与战略决策相关且能够评价的状态字段。对每个候选战略，BM 预测在固定时间跨度 $$H$$ 后这些字段的可能取值：

$$
\hat p\left(y_{t+H} \mid b_t, a_t^{(j)}\right)
$$

预测聚焦于会影响战略选择的变化，如科技和生产进展、兵力趋势、关键威胁、经济瓶颈、目标达成概率与主要风险，而不是复现完整 Observation。

BM 根据候选战略的预期目标进展、失败风险、不确定性和战略切换代价进行选择：

$$
a_t^* = \arg\max_{a_t^{(j)}}
\left(
\text{goal progress}
- \lambda_r \text{risk}
- \lambda_u \text{uncertainty}
- \lambda_s \text{switch cost}
\right)
$$

这些评分项应由结构化预测字段和预先定义的评价规则计算，而不是完全交给 BM 用自然语言自评。这样，未来预测才会直接影响当前战略，而不是成为不可验证的附加解释。

## 4. 战略一致性与近期执行

BM 维护一个轻量的战略承诺（Commitment），记录当前目标、关键假设和失效条件。上一轮战略始终作为下一轮候选之一：没有重要新证据时优先延续；出现明显反证、重大威胁或关键里程碑后再调整。

每轮决策分为三种结果：

- **continue**：目标和假设仍成立，继续当前战略；
- **refine**：总体目标不变，但调整优先级或执行重点；
- **replan**：原战略已失效，重新选择战略方向。

BM 可以推演较长时间的趋势，但只向 IM 发布最近一个战略周期的指导。IM 在该周期内产生具体动作，之后系统重新读取真实观测并规划，形成滚动时域控制：

$$
\text{reason for future, act for now, observe, then replan}
$$

## 5. 执行校准

BM 的战略不会被环境直接执行，而是经过 IM、动作校验、资源等待和 Ares Behavior 注册。因此，预测结果不能只与下一次观测直接比较，还必须考虑战略实际被执行到了什么程度。

现实链路为：

$$
a_t^*
\rightarrow
u_t^{IM}
\rightarrow
u_t^{runtime}
\rightarrow
o_{t+H}
\rightarrow
b_{t+H}
$$

其中 $$u_t^{IM}$$ 是 IM 提出的动作，$$u_t^{runtime}$$ 是运行时记录到的接受、排队、注册和持续执行轨迹；动作是否真正产生预期效果，仍由后续观测判断。

到达预测时间点后，系统比较预测与真实反馈，并生成结构化残差：

$$
r_{t+H} = R\left(
\hat y_{t+H},
y_{t+H}^{observed},
u_t^{IM},
u_t^{runtime}
\right)
$$

其中 $$y_{t+H}^{observed}$$ 是从新观测和更新后的 belief 中提取的可验证字段，不代表战争迷雾下的完整真实状态。

残差至少区分三种情况：

- **预测偏差**：战略得到基本执行，但局面发展与预测不符；
- **执行偏差**：IM 或运行时没有充分落实 BM 战略；
- **对手或外生变化、不可验证**：对手行为或外部局势发生变化，或战争迷雾使结果暂时无法判断。若几类原因无法可靠区分，应允许标记为 mixed 或 unknown，避免强行归因。

校准信息进入下一轮 BM 上下文，使 BM 能调整预测置信度和战略选择。这里的“校准”主要是基于对局反馈的上下文内校准，而不是假定模型参数已经在线更新。

## 6. 历史控制与真实观测锚定

系统不递归地把预测状态当作下一轮真实输入。每轮都必须从新的游戏观测重新构建当前认知，再以更新后的 belief 作为下一轮预测条件：

$$
b_{t+H} = U(b_t, o_{t+H}, u_t^{runtime}, \text{recent history}),
\qquad
\hat p(y_{t+2H} \mid b_{t+H}, a_{t+H})
$$

预测只用于规划和误差分析，不能替代真实观测。为避免上下文无限增长，BM 仅保留：

- 最近一次完整的战略、预测、执行结果和残差；
- 最近若干轮的压缩摘要；
- 少量长期校准结论。

## 7. 预期方法贡献

本设计拟通过实验验证的主要贡献可以概括为：

1. **预测驱动的战略选择**：对候选战略进行动作条件预测，使未来推演直接决定当前选择。
2. **部分可观测下的稳定规划**：结合 belief 与轻量 Commitment，在保持战略一致性的同时允许基于新证据及时调整。
3. **面向分层智能体的执行校准**：区分 BM 意图、IM 动作和运行时实际执行，避免把所有结果偏差都错误归因于战略模型。
4. **闭环滚动决策**：只执行近期战略，并持续使用真实观测重新锚定和规划，减少长程预测误差积累。

最终，EC-BM 不只是生成“下一步应该做什么”，而是持续回答三个问题：

1. 当前有哪些合理战略？
2. 每种战略最可能把局面带向哪里？
3. 上一轮结果不符合预期，是预测问题、执行问题，还是新的外部证据？

这三个问题共同构成一个简洁、可验证并适合消融实验的高级 BM 设计。
