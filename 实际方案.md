## 一、先明确“纯观测”的边界
输入严格拆成：

\[
\text{Agent Input}
=
o_t^{observation}
\oplus
m_t^{control}
\oplus
k^{knowledge}
\]

其中：

- \(o_t^{observation}\)：当前讨论的纯观测。
- \(m_t^{control}\)：Commitment、上一轮动作、执行残差。
- \(k^{knowledge}\)：战术表、动作表、规则知识。

纯 Observation 可以包括：

- 当前游戏直接提供的事实；
- 对这些事实进行无战略偏好的确定性聚合，例如计数、比例、区域归类；
- 最近直接观测到的变化和最后见到的信息。


## 二、理论上可取得的信息全集

项目通过 python-sc2 和 Ares 可以取得的信息大致如下。

| 信息类别 | 可用信息 | 处理建议 |
|---|---|---|
| 对局信息 | 游戏时间、迭代、地图、双方种族、出生点 | 保留时间和种族；地图信息只保留一次 |
| 资源 | 矿、气、收入速率 | 保留 |
| 人口 | 已用、上限、空余、工人、军队人口 | 保留，但去掉可推导冗余字段 |
| 经济 | 基地数、建造中基地、工人数、闲置工人、矿气分配、饱和缺口 | 保留 |
| 资源点 | 矿区剩余资源、气矿余量、可用矿点 | 仅保留基地级剩余资源比例 |
| 己方单位 | 类型、数量、ID、位置、血量、能量、订单、状态、角色 | 聚合；单体信息条件展开 |
| 己方建筑 | 类型、数量、建造进度、血量、位置、生产订单、附属建筑 | 数量和进度保留；位置和血量条件展开 |
| 生产状态 | 正在生产的单位、进度、生产建筑是否空闲 | 保留并聚合 |
| 科技状态 | 已完成升级、研究中升级、建筑科技状态 | 保留 |
| 技能状态 | Tactical Jump、Yamato 等是否可用 | 只保留关键单位的关键技能 |
| 敌方可见单位 | 类型、数量、位置、血量、活动状态 | 按区域聚合 |
| 敌方可见建筑 | 类型、区域、建造状态 | 保留 |
| 敌方历史证据 | 最后见到的类型、数量、区域、距今时间 | 限量保留 |
| 视野信息 | 哪些战略区域当前可见、多久未侦察 | 强烈建议新增 |
| 空间信息 | 坐标、基地、地图中心、扩张点、距离、路径 | 使用固定区域枚举；不提供完整坐标和网格 |
| 威胁信息 | 敌人是否靠近基地、空军/地面军数量 | 保留事实型聚合，不输出“疑似 rush”等判断 |
| 地图网格 | Pathing、influence、avoidance、creep | 不给 LLM，由运行时使用 |
| 作战模拟 | `can_win_fight`、安全点、路径代价 | 不属于纯观测 |
| Ares 情报判断 | Rush、Proxy、Greedy、Enemy Expanded | 不直接使用；保留其原始可见证据 |
| 时间变化 | 单位增减、建筑开工/完成、敌人首次出现、受到攻击 | 保留少量结构化事件 |
| 控制状态 | Ares role、Squad、动作历史、排队状态 | 放到执行上下文，不属于纯观测 |


## 三、信息聚合与抽象层级

系统增加两个简单的抽象层：

- **GroupRegistry**：将单位聚合为稳定的 `g1`、`g2`、`eg1` 等 Group。
- **SpatialRegistry**：将实际坐标映射为 `own_main`、`enemy_natural`、`map_center` 等稳定地标。

这些抽象只负责压缩信息和解析引用，内部划分、坐标及单位 Tag 均不暴露给 LLM。

### 1. Group 表示

每个 Group 只暴露以下信息：

1. **ID**：简短且稳定的标识，例如 `g1`、`eg1`。
2. **组成**：使用紧凑形式描述，例如 `BATTLECRUISER × 4`。
3. **Health**：使用组内单位总当前生命值除以总最大生命值，表示为百分比：
   $$
   H(G)=
   \frac{\sum_{u\in G}hp_u}
   {\sum_{u\in G}hp_u^{max}}
   \times 100\%
   $$
4. **能力**：默认不提供；只有确实影响当前决策的特殊能力才考虑加入。能力不作为主要动态预测字段。
5. **位置**：只描述为靠近哪个地标，例如 `near own_natural`。

示例：

```json
{
  "id": "g1",
  "composition": "BATTLECRUISER × 4",
  "health": "92%",
  "location": "near own_natural"
}
```

### 2. Group 划分逻辑

Group 的划分逻辑完全由运行时负责，不暴露给 LLM。

敌我双方都需要划分 Group，但敌方只保留决策所需的简要组成、Health 和位置，不提供过多细节。

初步划分规则如下：

1. 主要划分战斗单位。SCV 等采集单位由 Mining 和自动经济逻辑管理，默认不进入 Group。
2. 地面单位和空中单位必须分开，以便运行时选择对应的移动和寻路方式。
3. 拥有特殊技能或特殊用途的单位可以与普通作战单位分开，但不在第一版中进行过细划分。
4. 在以上分类基础上，根据单位靠近的地标进行简单空间聚合。

第一版应尽量减少 Group 数量。更细的单位职能和空间聚类规则保留为后续优化项。

### 3. Group ID 稳定性

Group ID 使用简短递增标识。相邻两次观测之间，根据新旧 Group 的成员重合程度继承 ID：

$$
J(G_{old},G_{new})
=
\frac{|G_{old}\cap G_{new}|}
{|G_{old}\cup G_{new}|}
$$

成员重合度最高的新 Group 继承旧 ID；新产生的 Group 分配新 ID。Group 合并时保留主要 Group 的 ID，分裂时由最大的子 Group 继承原 ID。

第一版不额外处理严重受伤、离群单位等特殊拆分情况。

### 4. 空间抽象

SpatialRegistry 提供一组固定地标：

```text
own_main
own_natural
own_third
own_fourth
map_center
enemy_fourth
enemy_third
enemy_natural
enemy_main
```

可按需补充：

```text
own_ramp
enemy_ramp
next_expansion
```

描述单位或 Group 的位置时，只选择距离最近的地标，例如：

```text
near own_main
near enemy_natural
near map_center
```

`near middle` 可以统一表示为 `near map_center`，避免出现同义字段。

如果一个位置明显远离所有已有地标，则暂时使用：

```text
elsewhere
```

表示中立野外区域。现阶段不额外设计“空地”或复杂地图区域系统；如果后续发现 Ares 或 MapAnalyzer 有稳定且通用的现成描述，再考虑接入。

### 5. 接口解析

动作接口同步使用 Group 和地标引用：

```json
{
  "id": "group.move",
  "args": {
    "group": "g1",
    "target": "enemy_natural"
  }
}
```

运行时负责完成转换：

- Group ID 转换为实际单位列表和 Unit Tags；
- 地标转换为对应的 `Point2`；
- Group 中心、空中或地面路径网格等参数由运行时自动推导。

LLM 不需要输出成员 ID、实际坐标、Group 中心或路径网格。
```